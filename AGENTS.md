# Repository Notes

- This repository is currently a scaffold: no application source, project manifest, CI workflow, or build/test/lint configuration is tracked.
- Do not assume a language, framework, package manager, or validation command. Add the relevant toolchain configuration before introducing implementation-specific commands or conventions.
- `README.md` only identifies the intended product as freelance account management; it does not define architecture or behavior.
- Keep environment secrets in ignored `.env` files. The repository `.gitignore` already excludes them.
